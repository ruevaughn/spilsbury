<%= form_for(@guestbook_signature) do |f| %>
  <% if @guestbook_signature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guestbook_signature.errors.count, "error") %> prohibited this guestbook_signature from being saved:</h2>

      <ul>
      <% @guestbook_signature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :guestbook_id %><br>
    <%= f.text_field :guestbook_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
